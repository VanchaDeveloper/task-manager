# Task Manager — Repository Overview 📁

This repository contains two main subprojects:

- `backend/` — Node + TypeScript API (Express, Drizzle ORM, Postgres)
- `frontend/` — Flutter mobile app

See detailed setup and run instructions in each subfolder:

- Backend: `backend/README.md`
- Frontend: `frontend/README.md`

Quick start:

- Start backend (Docker Compose):

```bash
cd backend
docker-compose up --build
```

- Start frontend (Flutter):

```bash
cd frontend
flutter pub get
flutter run
```
